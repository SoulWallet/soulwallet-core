{
    "name": "soulwalletcore",
    "scripts": {
        "build": "forge build --sizes",
        "test": "forge test -vvv",
        "gasCheck": "forge test -vv --match-contract 'GasCheckerTest' | grep -E 'gas checker|-----'"
    }
}